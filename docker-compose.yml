version: "3"

services:
    cloudmapper:
        build:
            context: .
        image: cloudmapper
        container_name: cloudmapper
        env_file: .env
        volumes:
            - ./config.json:/opt/cloudmapper/config.json
        ports:
            - ${EXTERNAL_PORT}:8000
